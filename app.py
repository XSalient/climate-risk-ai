import streamlit as st
st.set_page_config(page_title='Climate Risk PoC', layout='wide')
st.title('🌍 Climate Risk Intelligence — PoC (Merged)')
st.write('This is the merged PoC. Replace with full app code if needed (previous messages).')
